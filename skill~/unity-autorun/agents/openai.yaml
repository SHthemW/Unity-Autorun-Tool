interface:
  display_name: "Unity Autorun"
  short_description: "Check, navigate, and self-test Unity UI"
  default_prompt: "Use $unity-autorun to quickly check compatibility, then navigate, inspect UI state, and verify the requested result."

dependencies:
  tools:
    - type: "mcp"
      value: "unity-autorun"
      description: "Local Unity Autorun MCP server for UI navigation, inspection, and self-tests"
